<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<div class="container">
    <div class="row" >
        <div class="col-12" >
            {{#each pro as |x|}}
                <div class="card text-center mt-4 ml-2" style="display:inline-block;width:200px;">
                    <div class="card-header">
                        {{x.pname}}
                    </div>
                    <div class="card-body">
                        <a href="#" class="btn btn-primary">{{x.pprice}}</a>
                    </div>

                </div>

            {{/each}}

        </div>
        <a href="addproducts">add a product</a>
    </div>
</div>

</body>
</html>